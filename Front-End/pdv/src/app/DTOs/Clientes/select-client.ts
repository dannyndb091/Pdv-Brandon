export class SelectClient {
    clCode: string;
    clName: string;
    clMail: string;
    clCellphone: string;

    constructor(clCode: string, clName: string, clMail: string, clCellphone: string){
        this.clCode = clCode;
        this.clName = clName;
        this.clMail = clMail;
        this.clCellphone = clCellphone;
    }
}
